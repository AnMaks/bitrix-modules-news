# Модуль «Лента новостей (mynews)»

Данный модуль реализует простую ленту новостей на базе Highload-блока  
с возможностью перелистывания новостей по AJAX.

---

## 📌 Возможности модуля

- автоматическое создание Highload-блока новостей;
- добавление тестовых данных (8 новостей);
- вывод новостей по 2 штуки;
- переключение новостей без перезагрузки страницы;
- работа через контроллер модуля (BX.ajax.runAction);
- автоматическое создание публичной страницы `/news/`.

---

## ⚙️ Требования

Для установки модуля необходимы:

- доступ к серверу с установленным 1С-Битрикс;
- доступ к файловой системе сервера;
- права администратора в панели управления Битрикс;
- установленный модуль Highload-блоки.

---

## 🚀 Установка модуля

### 1. Доступ к серверу

Для установки модуля необходим доступ к серверу Bitrix  
(через SSH, FTP или файловый менеджер хостинга).

---

### 2. Копирование модуля

Скопируйте папку модуля:mynews.news в директорию: /local/modules/


---

### 3. Установка через административную панель

После копирования файлов:

1. Зайдите в административную часть сайта;
2. Перейдите в меню: Админ → Marketplace → Установленные решения
3. Найдите модуль: Лента новостей (mynews)
4. Нажмите «Установить».

---

### 4. Что происходит при установке

Во время установки модуль автоматически:

- регистрируется в системе;
- создаёт Highload-блок `mynews_news`;
- добавляет пользовательские поля (заголовок, текст, дата, сортировка);
- наполняет блок тестовыми новостями;
- копирует компонент в `/local/components`;
- создаёт публичную страницу: /news/

---

## 📰 Просмотр новостей

После установки модуль сразу готов к работе.

Откройте в браузере: http/<ip-site>/news/

На странице будет доступна лента новостей с кнопками переключения.

---

## 🧹 Удаление модуля

При удалении модуля через административную панель:

- удаляется Highload-блок новостей;
- удаляются тестовые данные;
- удаляется страница `/news/`;
- удаляется компонент модуля.

---